#include<bits/stdc++.h>
using namespace std;

int main(){
	vector<int>v; //DS  
    while(true){
        int n;
        cin>>n;
        if(n<0){
            break; //if the number is negatve stop taking input
        }
        else{
            v.push_back(n); //else push the element  in Data Structure
        }
    }

    // code
    
}